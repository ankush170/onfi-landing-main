import "@/styles/globals.css";
import type { AppProps } from "next/app";
import Head from "next/head";

export default function App({ Component, pageProps }: AppProps) {
  return (
    <>
      <Head>
        <title>OnFinance AI</title>
        <meta name="description" content="OnFinance AI - Explainable AI Agents for financial services. Get verifiable and controllable responses from LLMs on financial documents like regulatory circulars." />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <Component {...pageProps} />
    </>
  );
}
